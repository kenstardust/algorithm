dp=[ [ 0 in range(1.10)] in range(1,10)]
n=9

for k in range(n):
        for i in range(n):
            for j in range(n):
                if(dp[i][k]+dp[j][k] < dp[i][j]): 
                    dp[i][j] = dp[i][k]+dp[j][k]   
